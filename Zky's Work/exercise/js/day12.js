function simpleSolutions ()
{
    var x,y,z,a,b,c,d;
 
    x = 10;
    y = 35;
    z = x * y;

    // Problem ontop is multiplication

    c = "Bread"
    b = " Peanut butter"
    a = " Jam"
    d = c + b + a; //Bread Peanut butter Jam

    // problem ontop is how top make a simple snack
}